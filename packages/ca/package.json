{
  "private": true,
  "name": "@pngu/ca",
  "version": "1.0.0",
  "main": "lib/index.js",
  "typings": "lib/index.d.ts",
  "license": "MIT",
  "scripts": {
    "start": "yarn run clean && parcel index.html -p 8080 --open --log-level 3",
    "build": "yarn run clean && parcel build index.html -d out --no-source-maps --no-cache --detailed-report --public-url ./",
    "clean": "rimraf out lib dist .cache"
  },
  "dependencies": {
    "@pngu/core": "^1.0.0",
    "@thi.ng/hdom": "^5.0.7",
    "@thi.ng/interceptors": "^1.8.17",
    "@thi.ng/math": "^0.2.0",
    "@thi.ng/rstream-gestures": "^0.5.17",
    "tachyons": "^4.11.1"
  },
  "devDependencies": {
    "glslify-bundle": "^5.1.1",
    "glslify-deps": "^1.3.1"
  },
  "browserslist": [
    ">2%",
    "last 1 edge versions",
    "not ie > 0",
    "not op_mini all"
  ]
}
